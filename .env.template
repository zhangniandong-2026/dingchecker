# 环境变量配置模板

## Gemini API Key

请将以下内容添加到你的 `~/.zshrc` 或 `~/.bashrc`:

```bash
# Gemini API Key - 从 https://aistudio.google.com/app/apikey 获取
export GEMINI_API_KEY="your_api_key_here"
```

## 注意事项

⚠️ **安全提示**：
- 永远不要将API Key提交到Git仓库
- 不要在文档或代码中硬编码API Key
- 使用环境变量或配置文件（加入.gitignore）
- 定期轮换API Key

## 设置步骤

1. 获取API Key：
   访问 https://aistudio.google.com/app/apikey

2. 添加到环境变量：
   ```bash
   echo 'export GEMINI_API_KEY="your_api_key_here"' >> ~/.zshrc
   source ~/.zshrc
   ```

3. 验证配置：
   ```bash
   echo $GEMINI_API_KEY
   ```

## 软路由用户

如果使用软路由，不需要配置代理环境变量。
系统会自动检测并使用直连模式。
